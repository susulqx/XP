# BypassHook.java 代码深度解析报告

> 分析对象：`app/src/main/java/com/bypass/BypassHook.java`（通杀版）
> 交叉验证源：`app_b_smali/` 反编译代码
> 报告版本：修订版（含自检修正记录）

---

## 一、代码功能概述

### 1.1 总体目标

本模块是一个 Xposed 框架模块，用于在目标 App（包名 `app.unique.one`）运行时绕过其订阅验证机制，使 App 在**未安装配套验证 App（app.jjyy.passstore）** 的情况下仍能正常启动并显示内容。

### 1.2 与旧版的本质区别

| 维度 | 旧版（DeepSeek 编写） | 通杀版（本代码） |
|------|----------------------|-----------------|
| 类名定位 | 硬编码 `"zcc"` `"lf"` `"of8"` `"yp6"` | 运行时动态定位，零硬编码类名 |
| 方法名定位 | 硬编码 `"oO0OO0O"` `"O00OO"` 等 | 通过方法签名（参数+返回类型）定位 |
| 字段名定位 | 硬编码 `"oO0OO00"` `"oO0OOO"` 等 | 通过字段注解 `value()`（JSON key）定位 |
| 绕过路径 | 替换 of8.O00OO，模拟 App A 返回结果（6个Hook） | 写入 SP 使校验走"路径1"直接显示内容（2个Hook+SP写入） |
| 混淆鲁棒性 | 混淆变化即失效 | 依赖功能性字符串与类型特征，混淆不变 |

### 1.3 核心设计思想

利用目标 App 订阅校验逻辑中的一条**原生正常路径**（路径1）：

```
"oas"==1（已订阅） → 检查版本("osv") → 版本不满足 → 直接调用 O00OO0() 显示内容
```

通过向 SharedPreferences 写入 `"oas"=1` 和 `"osv"=0`，使校验流程自然走通这条路径，**无需替换任何方法体**，也**无需依赖 App A**。

---

## 二、关键模块说明

### 2.1 模块总览

```
BypassHook
├── handleLoadPackage()           — 入口：过滤目标包名，安装切入点 Hook
├── hookSharedPreferencesEntry()  — 切入点：Hook ContextImpl.getSharedPreferences
├── handleSpAccess()              — SP 访问处理：写入订阅状态 + 触发 Hook 安装
├── installHooks()                — Hook 安装编排：定位类 → 创建假数据 → Hook 方法
├── locateClassFromStack()        — 类定位：从调用栈找第一个非系统类（订阅管理类）
├── locateSubscriptionDataClass() — 类定位：扫描静态方法返回类型找订阅数据类
├── isSubscriptionDataClass()     — 类验证：7字段(3I+2Z+2J) + 注解 value 覆盖
├── getAnnotationValue()          — 注解读取：反射调用注解 value() 方法
├── createFakeSubscriptionData()  — 假数据创建：通过注解 value 设置字段
├── hookSubscriptionManagerMethods() — 方法 Hook：按签名定位并 Hook 读SP/获取数据方法
└── findStaticFieldByType()       — 字段查找：按类型定位静态缓存字段
```

### 2.2 入口模块：`handleLoadPackage()`（L74-87）

**职责**：Xposed 模块入口，过滤目标包名后安装切入点 Hook。

**逻辑**：
- 检查 `lpparam.packageName == "app.unique.one"`，不匹配则直接返回
- 获取 App 的 ClassLoader
- 调用 `hookSharedPreferencesEntry(cl)` 安装切入点

**自检**：包名 `"app.unique.one"` 是 App B 的 applicationId（见于 AndroidManifest.xml），是稳定标识。✅

### 2.3 切入点模块：`hookSharedPreferencesEntry()`（L96-123）

**职责**：Hook 系统 API `getSharedPreferences`，作为定位订阅管理类的切入点。

**逻辑**：
- 尝试 Hook 两个候选类：`android.app.ContextImpl` 和 `android.content.ContextWrapper`
- 仅 Hook `afterHookedMethod`（在原方法执行后介入）
- 每个 Hook 均委托给 `handleSpAccess()`

**设计考量**：
- 同时 Hook ContextImpl 和 ContextWrapper 是为了兼容性——不同 Android 版本或调用路径可能经过不同的类。ContextWrapper.getSharedPreferences 会委托给 ContextImpl，两者 Hook 可能重复触发，但 `handleSpAccess` 中有 `installed` 标志防止重复安装。✅

**自检修正**：初版报告未说明为何同时 Hook 两个类。修订补充：ContextWrapper 是 Activity/Service 的基类，某些调用路径经过它；ContextImpl 是实际实现。双 Hook 确保不遗漏。✅

### 2.4 SP 处理模块：`handleSpAccess()`（L125-152）

**职责**：当 `getSharedPreferences("app_setting", ...)` 被调用时，写入订阅状态并触发 Hook 安装。

**逻辑**：
1. 检查 `param.args[0]`（SP 文件名）是否等于 `"app_setting"`，不匹配则返回
2. 获取返回的 SharedPreferences 实例，写入 `"oas"=1` 和 `"osv"=0`
3. 双重检查锁（DCL）确保 `installHooks()` 只执行一次

**关键时序分析**：

```
App B 启动
  → of8.O00OO()                          [校验入口]
    → zcc.oO0OOO0("oas", 0)              [读SP "oas"]
      → zcc.oO0OOO()                     [获取SP实例]
        → ContextImpl.getSharedPreferences("app_setting", 0)
          ↓ afterHook 触发
          ← 写入 oas=1, osv=0            [本模块介入]
          ← installHooks()               [定位类并Hook]
        → 返回SP实例
      → SP.getInt("oas") = 1             [读到刚写入的1] ✅
    → v0=1 → :cond_0
    → zcc.oO0OOOo()                      [版本检查]
      → zcc.oO0OOO0("osv", 0)            [被Hook, 返回0]
      → 0 > 320 = false
    → :cond_1 → O00OO0()                 [显示内容] ✅
```

**自检**：`handleSpAccess` 在 `afterHookedMethod` 中执行，此时 `getSharedPreferences` 已返回 SP 实例。写入 SP 后，当前调用链中 `zcc.oO0OOO0` 继续执行 `sp.getInt("oas")`，会读到刚写入的 `1`。这是因为 `sp.edit().commit()` 是同步写盘，写完后后续 `getInt` 立即读到新值。✅

**自检修正**：初版报告未说明为何用 `commit()` 而非 `apply()`。修订补充：`commit()` 同步写盘，确保同一线程后续 `getInt` 读到新值；`apply()` 异步写盘，存在时序竞争风险。✅

### 2.5 类定位模块：`locateClassFromStack()`（L193-205）

**职责**：从当前线程调用栈中定位第一个非系统类，即订阅管理类（zcc）。

**原理**：`getSharedPreferences("app_setting")` 的直接调用者是 `zcc.oO0OOO()`。调用栈（从新到旧）为：

```
Thread.getStackTrace()
BypassHook.handleSpAccess()       [Xposed]
XposedBridge 内部                  [Xposed]
ContextImpl.getSharedPreferences() [Android]
zcc.oO0OOO()                       ← 第一个非系统类
zcc.oO0OOO0()
of8.O00OO()
of8.onViewCreated()
...
```

**逻辑**：遍历栈帧，用 `isSystemOrXposedClass()` 过滤系统/Xposed 类，返回第一个 App 类。

**自检**：`isSystemOrXposedClass()` 过滤了 `java.` `android.` `androidx.` `dalvik.` `de.robv.` `com.bypass.` `kotlin.` 等前缀。App B 的混淆类名（如 `zcc`）没有包前缀，不会被误过滤。✅

**边界情况**：如果栈帧中的类无法被 `cl.loadClass()` 加载（如已被卸载），代码跳过该帧继续查找，不会崩溃。✅

### 2.6 数据类定位与验证：`locateSubscriptionDataClass()` + `isSubscriptionDataClass()`（L225-279）

**职责**：定位订阅数据类（lf），通过反射特征匹配而非类名。

**定位逻辑**（`locateSubscriptionDataClass`）：
- 扫描 `zccClass.getDeclaredMethods()`
- 筛选条件：`static` + 无参 + 返回类型非基本类型/数组/String/Object
- 对每个候选返回类型调用 `isSubscriptionDataClass()` 验证

**验证逻辑**（`isSubscriptionDataClass`）：
1. `getDeclaredFields().length == 7`（恰好7个字段）
2. 字段类型计数：`3×int + 2×boolean + 2×long`（不匹配则返回 false）
3. 收集所有字段的注解 `value()`，验证覆盖全部7个 JSON key：`"st" "pu" "ex" "al" "ca" "v" "te"`

**交叉验证**（对照 `lf.smali`）：

| 字段（混淆名） | 类型 | 注解 value | 代码验证 |
|---------------|------|-----------|---------|
| oO0OO00 | int | "st" | ✅ intCount++ + "st" |
| oO0OO0O | boolean | "pu" | ✅ boolCount++ + "pu" |
| oO0OO0Oo | long | "ex" | ✅ longCount++ + "ex" |
| oO0OOO | boolean | "al" | ✅ boolCount++ + "al" |
| oO0OOO0 | long | "ca" | ✅ longCount++ + "ca" |
| oO0OOOO0 | int | "v" | ✅ intCount++ + "v" |
| oO0OooO0 | int | "te" | ✅ intCount++ + "te" |

合计：3 int + 2 boolean + 2 long = 7 字段，注解覆盖全部 key。✅

**自检修正**：初版报告称"7个实例字段"。修订：`getDeclaredFields()` 返回类声明的**所有**字段（含 static），不区分实例/静态。lf 类恰好没有 static 字段，所以 `length==7` 成立。但若未来 lf 添加 static 字段，此检查会失败。当前代码对此场景无防御——这是一个已知限制。✅（已标注）

### 2.7 注解读取模块：`getAnnotationValue()`（L285-298）

**职责**：读取字段的注解 `value()` 返回值。

**原理**：lf 字段使用 `@Lgrc;(value="st")` 注解（Gson 序列化注解）。注解类名 `grc` 会被混淆，但 Java 注解的 `value()` 方法名是 **Java 标准规范**，不会被混淆。

**逻辑**：
1. 遍历字段的所有声明注解
2. 对每个注解，尝试通过反射调用 `value()` 方法
3. 如果返回值是 String，则返回该值

**自检**：如果注解没有 `value()` 方法，`getDeclaredMethod("value")` 抛 NoSuchMethodException，被 catch 后跳过该注解。代码遍历所有注解，不会因单个注解无 value 而中断。✅

### 2.8 假数据创建模块：`createFakeSubscriptionData()`（L304-343）

**职责**：创建一个完整的"有效订阅"数据实例。

**逻辑**：
1. 通过无参构造函数实例化 lf 类
2. 遍历所有字段，通过注解 `value()` 确定字段含义
3. 根据 JSON key 设置有效值：

| JSON key | 语义 | 设置值 | 依据 |
|----------|------|-------|------|
| "st" | 订阅状态 | 1（有效） | of8.O00OO 中 `if-ne v0, v2(:cond_0)` 要求 status==1 |
| "al" | 是否激活 | true | yp6.smali :pswitch_2 中检查 isActive |
| "pu" | 是否高级 | true | 功能门控 |
| "ex" | 过期时间 | Long.MAX_VALUE | 永不过期 |
| "ca" | 创建时间 | 当前时间 | 防御性字段 |
| "v" | 版本号 | 1 | 防御性字段 |
| "te" | 类型 | 0（标准） | yp6.smali :pswitch_2 中 type 检查 |

**自检**：`f.setAccessible(true)` 确保能写 private 字段。`switch(val)` 使用 String 比较，Java 7+ 支持。无参构造函数 `getDeclaredConstructor()` 在 lf.smali 中确认存在。✅

### 2.9 方法 Hook 模块：`hookSubscriptionManagerMethods()`（L355-413）

**职责**：Hook 订阅管理类的关键方法，作为 SP 写入的兜底措施。

**Hook 策略**（通过方法签名定位，不依赖方法名）：

| 签名 | 原方法名 | Hook 行为 | 目的 |
|------|---------|----------|------|
| `(String, int) → int` | oO0OOO0 | key=="oas"→返回1, key=="osv"→返回0 | 防止SP被覆盖 |
| `() → lf` | oO0OO0O | 返回假lf + 更新静态缓存 | 防止数据被重置 |

**签名唯一性验证**（对照 `zcc.smali` 的全部方法）：

| 方法 | 参数 | 返回 | 是否匹配Hook1 | 是否匹配Hook2 |
|------|------|------|:---:|:---:|
| oO0OO0O() | () | Llf | ✗ | ✅ |
| oO0OO0Oo(String,Z) | (String,boolean) | boolean | ✗ | ✗ |
| oO0OOO() | () | SharedPreferences | ✗ | ✗ |
| oO0OOO0(String,I) | (String,int) | int | ✅ | ✗ |
| oO0OOOO() | () | boolean | ✗ | ✗ |
| oO0OOOO0() | () | boolean | ✗ | ✗ |
| oO0OOOo() | () | boolean | ✗ | ✗ |
| oO0OOo(String,I) | (String,int) | void | ✗ | ✗ |
| oO0OOo0(String,Z) | (String,boolean) | void | ✗ | ✗ |
| oO0OOoO(String,String) | (String,String) | void | ✗ | ✗ |
| oO0OooO0(String,String) | (String,String) | String | ✗ | ✗ |

**结论**：每个签名仅匹配一个方法，定位精确。✅

**`()→lf` Hook 的额外行为**：在返回假数据前，通过 `findStaticFieldByType()` 找到 lf 类型的静态字段（即 `zcc.oO0OOO0:Llf;` 缓存字段），将假数据写入缓存。这防止其他代码通过缓存字段读到空值或旧值。✅

**自检修正**：初版报告未说明 `(String,int)→int` Hook 使用 `XC_MethodHook`（before 拦截）而非 `XC_MethodReplacement`（完全替换）。修订补充：使用 `beforeHookedMethod` + `setResult` 只在 key 匹配时拦截，其他 key 放行原方法。这保证非订阅相关的 SP 读取（如其他配置项）正常工作。✅

---

## 三、核心逻辑流程梳理

### 3.1 整体流程图

```
[Xposed 加载]
    │
    ▼
handleLoadPackage()
    │ 包名 == "app.unique.one"?
    ├─ 否 → return
    └─ 是
        │
        ▼
hookSharedPreferencesEntry()
    │ Hook ContextImpl.getSharedPreferences (after)
    │ Hook ContextWrapper.getSharedPreferences (after)
    │
    ▼
[等待 App B 启动...]
    │
    ▼
App B: of8.onViewCreated() → of8.O00OO()
    │
    ▼
zcc.oO0OOO0("oas", 0) → zcc.oO0OOO()
    │
    ▼
ContextImpl.getSharedPreferences("app_setting", 0)
    │
    ▼ afterHook 触发
handleSpAccess()
    │ name == "app_setting"?
    ├─ 否 → return（放行）
    └─ 是
        │
        ├─ SP 写入: oas=1, osv=0
        │
        └─ installed? ── 是 ──→ return
              │
              否
              │
              ▼ (synchronized DCL)
        installHooks()
              │
              ├─ ① locateClassFromStack() → zccClass
              ├─ ② locateSubscriptionDataClass() → llfClass
              ├─ ③ createFakeSubscriptionData() → fakeLlf
              └─ ④ hookSubscriptionManagerMethods()
                    ├─ Hook (String,int)→int
                    └─ Hook ()→lf
              │
              ▼ installed = true
        [Hook 安装完成]
    │
    ▼ [getSharedPreferences 返回]
zcc.oO0OOO0 继续: SP.getInt("oas") = 1  ← 读到写入的1
    │
    ▼
of8.O00OO: v0=1 → :cond_0
    │
    ▼
zcc.oO0OOOo() → zcc.oO0OOO0("osv", 0) → Hook返回0 → 0>320=false
    │
    ▼
:cond_1 → O00OO0()  ← 显示内容 ✅
```

### 3.2 路径1绕过原理详解

目标 App 的校验入口 `of8.O00OO()` 有以下分支逻辑（基于 smali 反编译）：

```
v0 = 读SP("oas")
    ├─ v0 == 0 → :cond_2（未订阅 → 走 ActivityResult/AIDL → 需 App A）
    ├─ v0 == 1 → :cond_0（已订阅）
    │              ├─ oO0OOOo() == true  → return（已校验，什么都不做）
    │              └─ oO0OOOo() == false → :cond_1 → O00OO0() ← 显示内容 ✅
    └─ 其他 → return
```

本模块选择**路径1**（`:cond_0 → :cond_1 → O00OO0()`），条件为：
- `"oas" == 1`：通过 SP 写入 + 方法 Hook 双重保证
- `oO0OOOo() == false`：该函数读取 `"osv"` 并比较 `> 320`，`"osv"=0` 使其返回 false

**为何选择路径1**：
- 路径1是 App 的**原生正常逻辑**（已订阅但版本不满足时显示内容），不触发任何异常或对话框
- 不依赖 ActivityResult（需 App A 存在）
- 不依赖 AIDL Service（需 App A 存在）
- 只需控制两个 SP 值，介入面最小

### 3.3 时序正确性验证

| 步骤 | 事件 | 验证 |
|------|------|------|
| T0 | Xposed handleLoadPackage 执行 | 在 Application.onCreate 之前 ✅ |
| T1 | ContextImpl.getSharedPreferences Hook 安装 | T0 之后立即执行 ✅ |
| T2 | App B 启动，of8.onViewCreated 触发 | T1 之后 ✅ |
| T3 | of8.O00OO 调用 zcc.oO0OOO0("oas") | T2 之后 ✅ |
| T4 | zcc.oO0OOO 首次调用，触发 getSharedPreferences | T3 之后 ✅ |
| T5 | afterHook 写入 oas=1, osv=0 | T4 之后，getSharedPreferences 返回前 ✅ |
| T6 | installHooks 完成（定位+Hook） | T5 之后，同一线程同步执行 ✅ |
| T7 | getSharedPreferences 返回 SP 实例 | T6 之后 ✅ |
| T8 | zcc.oO0OOO0 继续: SP.getInt("oas") | T7 之后，读到 T5 写入的 1 ✅ |
| T9 | of8.O00OO: v0=1 → :cond_0 | T8 之后 ✅ |
| T10 | zcc.oO0OOOo() → oO0OOO0("osv") → Hook返回0 | T9 之后，T6 已安装 Hook ✅ |
| T11 | :cond_1 → O00OO0() → 显示内容 | T10 之后 ✅ |

**关键时序保证**：T5（SP写入）在 T8（SP读取）之前，因为它们在同一线程的同一调用链中。T6（Hook安装）在 T10（Hook生效）之前，因为 T6 在 T4 的 afterHook 中同步完成。✅

---

## 四、稳定特征清单与混淆鲁棒性分析

### 4.1 依赖的稳定特征

| 特征 | 类型 | 为何不随混淆变化 | 用于 |
|------|------|----------------|------|
| `"app_setting"` | SP 文件名 | 功能性字符串，混淆会导致 SP 读写失败 | 切入点过滤 |
| `"oas"` / `"osv"` | SP key | 功能性字符串，混淆会导致状态读取失败 | 订阅状态控制 |
| `"st"/"pu"/"ex"/"al"/"ca"/"v"/"te"` | 注解 value | JSON 序列化 key，混淆会导致 JSON 解析失败 | lf 类定位与字段设置 |
| `value()` 方法名 | 注解方法 | Java 注解标准方法名，编译器/混淆器不修改 | 注解值读取 |
| 3×int + 2×boolean + 2×long | 字段类型组合 | 字段类型由业务逻辑决定，混淆不改类型 | lf 类验证 |
| `(String,int)→int` | 方法签名 | 参数/返回类型由业务逻辑决定 | zcc 方法定位 |
| `()→lf类型` | 方法签名 | 返回类型由业务逻辑决定 | zcc 方法定位 |

### 4.2 混淆鲁棒性自检

| 混淆变化场景 | 是否影响本模块 | 原因 |
|-------------|:---:|------|
| 类名变化（zcc→xxx） | ❌ 不影响 | 通过调用栈动态定位 |
| 方法名变化（oO0OOO0→xxx） | ❌ 不影响 | 通过签名定位 |
| 字段名变化（oO0OO00→xxx） | ❌ 不影响 | 通过注解 value 定位 |
| 注解类名变化（grc→xxx） | ❌ 不影响 | 通过反射调用 value()，不依赖注解类名 |
| SP 文件名变化 | ⚠️ 影响 | 切入点失效（但功能性字符串通常不变） |
| JSON key 变化 | ⚠️ 影响 | lf 定位失效（但 JSON 协议通常不变） |
| 字段类型/数量变化 | ⚠️ 影响 | lf 验证失效（需更新验证逻辑） |

**结论**：本模块对所有名称级混淆（类名/方法名/字段名/注解类名）完全免疫。仅在结构性变化（SP文件名/JSON协议/字段结构）时需更新，而这些是功能性约束，混淆器不会修改。✅

---

## 五、自检与修订记录

### 5.1 初版报告不足之处

| 编号 | 初版问题 | 修订内容 |
|------|---------|---------|
| S1 | 未说明为何同时 Hook ContextImpl 和 ContextWrapper | 补充：兼容不同调用路径，DCL 防重复 |
| S2 | 未说明为何用 commit() 而非 apply() | 补充：commit() 同步写盘，避免时序竞争 |
| S3 | 称"7个实例字段"不准确 | 修正：getDeclaredFields 返回所有声明字段（含static），lf 恰好无 static 字段 |
| S4 | 未说明 (String,int)→int 为何用 beforeHook 而非 Replacement | 补充：before+setResult 只拦截目标 key，放行其他 key |
| S5 | 未验证签名唯一性 | 补充：对照 zcc.smali 全部11个方法的签名匹配表 |
| S6 | 未分析 ()→lf Hook 的缓存更新逻辑 | 补充：findStaticFieldByType 更新 zcc.oO0OOO0 缓存字段 |

### 5.2 已知限制

1. **lf 含 static 字段时验证失败**：`isSubscriptionDataClass` 检查 `fields.length == 7`，若 lf 新增 static 字段会不匹配。当前 lf 无 static 字段，不受影响。
2. **功能门控副作用**：路径1下 `oO0OOOo()` 返回 false，导致 `oO0OOOO0()`（被搜索/扫码等功能门控调用）也返回 false，这些功能可能受限。但 App 能正常启动显示内容。
3. **构建环境**：本环境无 Android SDK 和 JDK 17+，未执行编译验证。代码语法经人工审查，依赖 Xposed API 82。

---

## 六、文件清单

```
xposed_module_v1/
├── app/
│   ├── build.gradle                    # AGP 8.7, compileSdk 34, Xposed API 82
│   └── src/main/
│       ├── AndroidManifest.xml         # xposedmodule=true, scope=app.unique.one
│       ├── assets/xposed_init          # → com.bypass.BypassHook
│       ├── java/com/bypass/
│       │   └── BypassHook.java         # 通杀版核心（本报告分析对象）
│       └── res/values/
│           └── arrays.xml              # xposed_scope
├── build.gradle
├── settings.gradle
├── gradle.properties
└── gradle/wrapper/gradle-wrapper.properties  # Gradle 8.9
```
